// Placeholder admin dashboard so the login flow has somewhere to land.
// Phase 6 replaces this with the pending queue and the sync-failure banner.
export default function AdminDashboard() {
  return (
    <div className="flex flex-col gap-2">
      <h2 className="text-xl font-bold tracking-tight">Queue</h2>
      <p className="text-ink-muted">Coming soon.</p>
    </div>
  );
}
